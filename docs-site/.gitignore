# Build artifact: copied from api/openapi/v1.yaml by `make docs`.
# Regenerated on every build and in CI, so it is not committed.
openapi.yaml

# Local site identity (your legal name etc.); never commit it. See site.config.example.
site.config

# Build artifacts: rendered from legal/*.template.html by build.sh, like openapi.yaml.
terms.html
privacy.html
refund.html
